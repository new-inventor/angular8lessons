import { DottedPipePipe } from './dotted-pipe.pipe';

describe('DottedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DottedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
